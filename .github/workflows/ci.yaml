name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  prepare:
    name: 'Prepare workflow'
    uses: eliashaeussler/gha/.github/workflows/preparation.yaml@261c9009ac452372d06c5dae29b9c9496d15f9d7 # 0.1.0

  checks:
    name: 'Checks'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    permissions:
      security-events: write

    uses: eliashaeussler/gha/.github/workflows/checks.yaml@261c9009ac452372d06c5dae29b9c9496d15f9d7 # 0.1.0
    with:
      # We use the minimum supported PHP version
      php-version: 8.2 # renovate-ignore
      npm: true

  unit-tests:
    name: 'Unit tests (PHP ${{ matrix.php-version }}, ${{ matrix.os }} & ${{ matrix.dependencies }} dependencies)'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    strategy:
      fail-fast: false
      matrix:
        php-version: ["8.2", "8.3", "8.4", "8.5"]
        dependencies: ["highest", "lowest"]
        os: ["Ubuntu"]
        include:
          - php-version: "8.5"
            dependencies: "highest"
            os: "macOS"
          - php-version: "8.5"
            dependencies: "highest"
            os: "Windows"

    uses: eliashaeussler/gha/.github/workflows/composer-tests.yaml@261c9009ac452372d06c5dae29b9c9496d15f9d7 # 0.1.0
    with:
      php-version: ${{ matrix.php-version }}
      dependencies: ${{ matrix.dependencies }}
      os: ${{ matrix.os }}
      command: 'test:unit'

  e2e-tests:
    name: 'E2E tests'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    uses: eliashaeussler/gha/.github/workflows/composer-tests.yaml@261c9009ac452372d06c5dae29b9c9496d15f9d7 # 0.1.0
    with:
      command: 'test:e2e'

  test-coverage:
    name: 'Test coverage'

    needs: [prepare]
    if: ${{ needs.prepare.outputs.continue == 'true' }}

    uses: eliashaeussler/gha/.github/workflows/composer-test-coverage.yaml@261c9009ac452372d06c5dae29b9c9496d15f9d7 # 0.1.0
